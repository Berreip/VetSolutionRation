namespace VSR.WPF.Utils.Adapters.CalculationAdapters;

/// <summary>
/// Specific interface that define all items that could be added to calculation panel
/// </summary>
public interface IAdapterInCalculation
{
    bool IsSelected { get; }
}